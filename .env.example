# ── Database ──────────────────────────────────────────────────
# On Render this is auto-injected from the vij-db Postgres addon.
# For local dev, point to your own Postgres instance.
DATABASE_URL=postgresql://user:password@localhost:5432/venezuela_journal

# ── Storage ───────────────────────────────────────────────────
STORAGE_DIR=./storage
LOG_LEVEL=INFO

# ── Scraper settings ─────────────────────────────────────────
SCRAPER_TIMEOUT_SECONDS=30
SCRAPER_MAX_RETRIES=3
SCRAPER_RETRY_DELAY_SECONDS=60
SCRAPER_LOOKBACK_DAYS=30

# ── Tesseract (OCR for gazette PDFs) ─────────────────────────
TESSERACT_CMD=/usr/local/bin/tesseract
TESSERACT_LANG=spa

# ── OpenAI (REQUIRED — powers the LLM investor analysis) ────
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# ── Buttondown (subscriber signup from the website) ──
# Get a key at https://buttondown.com/settings/programming
BUTTONDOWN_API_KEY=

# ── Newsletter (optional — set to "sendgrid" to send real emails) ──
# While set to "console", emails are printed to logs only.
NEWSLETTER_PROVIDER=console
NEWSLETTER_FROM_EMAIL=briefing@venezuelanbusiness.net
# Get a key at https://app.sendgrid.com/settings/api_keys
NEWSLETTER_API_KEY=SG...
SUBSCRIBER_LIST_PATH=subscribers.json

# ── Server ────────────────────────────────────────────────────
SERVER_PORT=8080
